"""Web UI package: FastAPI server + static frontend.

Run with ``python -m multi_agent_cad.web`` (delegates to
``multi_agent_cad.web.server:main``). Local single-user use only: the pipeline
executes generated Python with the launching user's privileges.
"""
